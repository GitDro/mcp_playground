# MCP Arena Local Development Configuration
# Copy this to .env and customize as needed for LOCAL development

# =============================================================================
# SERVER CONFIGURATION (for local HTTP testing)
# =============================================================================
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=INFO

# CORS Configuration (mainly for local HTTP testing)
ENABLE_CORS=true

# =============================================================================
# TOOL CONFIGURATION
# =============================================================================

# YouTube transcript processing - max tokens before truncation
YOUTUBE_MAX_TOKENS=24000

# WebShare proxy configuration for YouTube transcript API (bypasses cloud IP blocking)
# Format: comma-separated list of IP:PORT:USERNAME:PASSWORD entries
# Example: WEBSHARE_PROXIES="1.2.3.4:8080:user1:pass1,5.6.7.8:8080:user2:pass2"
WEBSHARE_PROXIES="your_proxy_list_here"

# Cache directory for storing temporary data
CACHE_DIRECTORY=cache

# =============================================================================
# RETRY SYSTEM CONFIGURATION
# =============================================================================

# Automatic retry system for enhanced tool reliability  
MCP_RETRY_MAX_ATTEMPTS=3
MCP_RETRY_BASE_DELAY=0.5
MCP_RETRY_TYPE_COERCION=true
MCP_RETRY_LOGGING=true

# =============================================================================
# USAGE NOTES
# =============================================================================

# Local Development:
# - Copy this file to .env
# - Run: uv run python mcp_server.py (for Claude Desktop)
# - Run: uv run python -m src.server http 8000 (for HTTP testing)

# Cloud Deployment:
# - FastMCP Cloud handles most settings automatically
# - Usually no .env file needed for cloud deployment at all!